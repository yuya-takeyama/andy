%h2 Trunk
%ul
  %li!= link_to 'trunk', "#{@project.absolute_path}/trunk"

%h2 Branches
- if @repo.branches.empty?
  %p No branches are available.
- else
  %ul
    - @repo.branches.each do |branch|
      %li!= link_to branch, "/projects/#{@project.id}/branches/#{branch}"

%h2 Tags
- if @repo.tags.empty?
  %p No tags are available.
- else
  %ul
    - @repo.tags.each do |tag|
      %li!= link_to tag, "/projects/#{@project.id}/tags/#{tag}"
